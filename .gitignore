# Foundry
out/
cache/
broadcast/
# Foundry deps only. Anchored: an unanchored "lib/" also ate app/src/lib.
/lib/

# Node
node_modules/
dist/
.env
.env.local

# Editor / OS
.DS_Store
.fuse_hidden*

# Analyser output (regenerate with `aderyn .`)
report.md

# Claude scratch, not part of the source
docs/reference/
docs/_to_delete/

# Working docs and launch notes. Held back until after launch -- publishing
# is one-way, adding them later is not. Delete these two lines to include.
docs/
